#include<iostream>
using namespace std;
int main()
{
    long int count,i;
    cin>>count;
    for(i=0;i<count;i++){
    long int x,y,z;
    cin>>x>>y>>z;
    if(abs(x-z)>abs(y-z))
    cout<<"Cat B"<<endl;
    else if(abs(y-z)>abs(x-z))
    cout<<"Cat A"<<endl;
    else cout<<"Mouse C"<<endl;}
}
